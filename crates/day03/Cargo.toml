[package]
name = "day03"
version = "0.2.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
regex = { workspace = true, optional = true }
helpers = { path = "../helpers"}

[features]
regex=["dep:regex"]